/**
 * Expression is which produce some value
        # 2 + 2
        # 3 * 7
        # 25 / 5
        # 4 + 2 + 8 * (8 ** 9) - Math.sqrt(4.0)
* Statement is that somthing that can execute an one more action
        # var assignValue =10 + 20;
        #  var Obj = {
            name : xyz,
            temp : function(){
                console.log("any thing");
            }
        }
        # for (let index = 0; index < array.length; index++) {
    const element = array[index];
        }
        # if (condition) {
    
            } else {
    
        }
        # console.log("any thing");
        */
